<div class="container mt-5">
  <div class="card shadow p-4">
    <h3 class="text-danger text-center mb-4">¿Está seguro que desea eliminar este producto?</h3>

    <div class="mb-4 text-center">
      <p><strong>Nombre:</strong> {{product.name}}</p>
      <p><strong>Descripción:</strong> {{product.description}}</p>
      <p><strong>Precio:</strong> RD$ {{product.price}}</p>

      {{#if product.image}}
        <img src="/images/{{product.image}}" alt="Imagen del producto" class="img-thumbnail mt-3" style="max-width: 200px;">
      {{else}}
        <p class="text-muted mt-3">Este producto no tiene imagen</p>
      {{/if}}
    </div>

    <form action="/merchant/products/delete" method="POST" class="text-center">
      <input type="hidden" name="_csrf" value="{{csrfToken}}">
      <input type="hidden" name="id" value="{{product.id}}">

      <button type="submit" class="btn btn-danger px-4 mr-2">Eliminar</button>
      <a href="/merchant/products" class="btn btn-secondary px-4">Cancelar</a>
    </form>
  </div>
</div>


